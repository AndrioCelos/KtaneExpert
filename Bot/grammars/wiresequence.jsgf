#JSGF V1.0 UTF-8 en;
grammar wiresequence;
import <main.*>;
import <util.*>;

public <root> = <command> | <wireflags> (then <wireflags>)*;

<wireflags> = (red | blue | black) [to] (alfa | bravo | charlie);
